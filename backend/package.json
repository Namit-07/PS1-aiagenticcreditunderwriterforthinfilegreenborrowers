{
  "name": "@acu/backend",
  "version": "0.1.0",
  "private": true,
  "description": "FastAPI main backend (Team A). This package.json is a Turborepo task-runner shim only; all real deps live in requirements.txt.",
  "scripts": {
    "install:dev": "python -m pip install -r requirements.txt",
    "dev": "uvicorn app.main:app --reload --port 8000",
    "serve": "uvicorn app.main:app --host 0.0.0.0 --port 8000",
    "test": "python -m pytest tests",
    "lint": "python -m compileall -q app tests || exit 0"
  }
}